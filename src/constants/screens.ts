const Screens = {
    // =========================================================================
    // Auth
    // =========================================================================
    HOME: "/home",
    REPORT: "/report",
    CREATE_REPORT: "/report/create",
    UPDATE_REPORT: "/report/update",
    ABSENSE: "/absence",
    ACCOUNT: "/account",

    // =========================================================================
    // Public
    // =========================================================================
    LOGIN: "/login"
};

export default Screens;
